# FROM SHINY PACKAGE, WITH MODIFICATIONS

asNamespace("shiny")[["normalizeChoicesArgs"]]
asNamespace("shiny")[["generateOptions"]]
asNamespace("shiny")[["isGroup"]]
asNamespace("shiny")[["hasGroups"]]
asNamespace("shiny")[["asNamed"]]
asNamespace("shiny")[["setDefaultNames"]]
asNamespace("shiny")[["asCharacter"]]
asNamespace("shiny")[["processFlatChoices"]]
asNamespace("shiny")[["processGroupedChoices"]]
asNamespace("shiny")[["choicesWithNames"]]
