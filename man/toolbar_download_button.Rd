% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolbar.R
\name{toolbar_download_button}
\alias{toolbar_download_button}
\alias{update_toolbar_download_button}
\title{Toolbar Download Button}
\usage{
toolbar_download_button(
  outputId,
  label = "Download",
  icon = shiny::icon("download"),
  show_label = FALSE,
  tooltip = !show_label,
  ...,
  disabled = FALSE,
  border = FALSE
)

update_toolbar_download_button(
  outputId,
  disabled = NULL,
  session = get_current_session()
)
}
\arguments{
\item{outputId}{The download output ID (connects to \code{\link[shiny:downloadHandler]{shiny::downloadHandler()}} in server).}

\item{label}{The button label. By default, \code{label} is not shown but is used by
\code{tooltip}. Set \code{show_label = TRUE} to show the label.}

\item{icon}{An icon. Defaults to \code{shiny::icon("download")}.}

\item{show_label}{Whether to show the label text. If \code{FALSE} (the default),
only the icon is shown. If \code{TRUE}, the label text is shown alongside the icon.}

\item{tooltip}{Tooltip text to display when hovering. Can be:
\itemize{
\item \code{TRUE} (default when \code{show_label = FALSE}) - shows tooltip with \code{label} text
\item \code{FALSE} (default when \code{show_label = TRUE}) - no tooltip
\item A character string - shows tooltip with custom text
}}

\item{...}{Additional attributes passed to the \verb{<a>} tag.}

\item{disabled}{If \code{TRUE}, the button will not be clickable. Since \verb{<a>} tags
have no native disabled attribute, this adds \code{class="disabled"},
\code{aria-disabled="true"}, and \code{tabindex="-1"}.}

\item{border}{Whether to show a border around the button.}

\item{session}{A Shiny session object.}
}
\value{
Returns a download button suitable for use in a toolbar.
}
\description{
A download button designed to fit well in small places such as in a \code{\link[=toolbar]{toolbar()}}.
}
\section{Functions}{
\itemize{
\item \code{update_toolbar_download_button()}: Update a toolbar download button.

}}
\examples{
\dontshow{if (rlang::is_interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# Download button in a card toolbar
card(
  card_header(
    "Flower Data",
    toolbar(
      align = "right",
      toolbar_download_button("download_data", label = "Download")
    )
  )
)
\dontshow{\}) # examplesIf}
}
\seealso{
Other toolbar components:
\code{\link{toolbar}()},
\code{\link{toolbar_divider}()},
\code{\link{toolbar_input_button}()},
\code{\link{toolbar_input_select}()}
}
\concept{toolbar components}
