% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offcanvas.R
\name{show_offcanvas}
\alias{show_offcanvas}
\alias{hide_offcanvas}
\alias{toggle_offcanvas}
\title{Show, hide, or toggle an offcanvas from the server}
\usage{
show_offcanvas(offcanvas, ..., session = get_current_session())

hide_offcanvas(id, ..., session = get_current_session())

toggle_offcanvas(id, show = NULL, ..., session = get_current_session())
}
\arguments{
\item{offcanvas}{An \code{\link[=offcanvas]{offcanvas()}} object, or a string / tags that will be
converted to an offcanvas with default settings.}

\item{...}{Currently ignored; must be empty.}

\item{session}{A Shiny session object (the default should almost always be
used).}

\item{id}{The \code{id} of an offcanvas in the UI. \code{hide_offcanvas()} also accepts
an \code{\link[=offcanvas]{offcanvas()}} object whose \code{id} was set.}

\item{show}{Whether to show (\code{TRUE}) or hide (\code{FALSE}) the offcanvas. The
default (\code{NULL}) shows it if hidden and hides it if shown.}
}
\value{
Invisibly returns the offcanvas \code{id} (the generated id when
\code{show_offcanvas()} is given an object without one).
}
\description{
Control an \code{\link[=offcanvas]{offcanvas()}} panel from a Shiny server function. \code{show_offcanvas()}
takes an offcanvas \strong{object} (and will render it into the page if it isn't
already there); \code{hide_offcanvas()} and \code{toggle_offcanvas()} act on a panel
already in the UI, by \strong{id}.
}
\section{Functions}{
\itemize{
\item \code{show_offcanvas()}: Render (if needed) and show an offcanvas.

\item \code{hide_offcanvas()}: Hide an offcanvas by \code{id} (or object).

\item \code{toggle_offcanvas()}: Toggle an offcanvas by \code{id}.

}}
\section{Module IDs}{

When controlling an offcanvas from a Shiny module, pass the
\strong{module-local} id to \code{hide_offcanvas()} / \code{toggle_offcanvas()} — the
same bare id you would use with \code{input[[id]]}. \code{shiny::NS()} /
\code{session$ns()} is only applied when \emph{creating} the panel's \code{id} in the UI.
\code{show_offcanvas()} returns the local id, so it round-trips cleanly to
\code{hide_offcanvas()} / \code{toggle_offcanvas()}. Do not pass a UI object whose
id was already namespaced back to a server verb — it would double-namespace.
}

\examples{
\dontshow{if (rlang::is_interactive()) withAutoprint(\{ # examplesIf}
library(shiny)
library(bslib)

ui <- page_fluid(
  actionButton("show", "Show"),
  actionButton("hide", "Hide")
)
server <- function(input, output, session) {
  # Defined and revealed entirely from the server
  observeEvent(input$show, {
    show_offcanvas(
      offcanvas("Revealed from the server.", title = "Notice", id = "note")
    )
  })
  observeEvent(input$hide, hide_offcanvas("note"))
}
shinyApp(ui, server)
\dontshow{\}) # examplesIf}
}
\seealso{
Other Offcanvas components:
\code{\link[=offcanvas]{offcanvas()}}
}
\concept{Offcanvas components}
